#include "main.h"
#include <string.h>
/**
 * _strlen - a function
 * Return: 1 if c uppercasetrue, 0 otherwise.
 * @s: this is a var
 */
int _strlen(char *s)
{
int length = strlen(str);
return (length);
}
