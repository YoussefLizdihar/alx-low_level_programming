#include "main.h"

/**
 * print_chessboard - a function
 * Return: 1 if true, 0 otherwise.
 * @a: this is a var
 */

void print_chessboard(char (*a)[8])
{
int i = 0;
int n = 0;
while (i < 8)
{
while (n < 8)
{
putchar(a[i][n]);
n++;
if (n / 8)
{
putchar('\n');
}
}
n = 0;
i++;
}
}
