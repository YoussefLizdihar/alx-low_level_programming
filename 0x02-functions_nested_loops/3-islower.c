#include "main.h"
#include <ctype.h>
/**
 * _islower - a function that checks for lowercase character
 * Return: success if 1.
 */
int _islower(int c)
{
if (islower(c))
{
return (1);
}
else
{
return (0);
}
}
