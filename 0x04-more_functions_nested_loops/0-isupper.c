#include "main.h"
#include <ctype.h>
/**
 * _isupper - a function that checks for uppercase character
 * Return: 1 if c uppercase, 0 otherwise.
 * @c: this is the char
 */
int _isupper(int c)
{
if (isupper(c))
{
return (1);
}
else
{
return (0);
}
}
