#include "main.h"
#include <ctype.h>
/**
 * _isdigit - a function that checks for lowercase character
 * Return: 1 if c is a digit, 0 otherwise.
 * @c: this is the char
 */
int _isdigit(int c)
{
if (isdigit(c))
{
return (1);
}
else
{
return (0);
}
}
